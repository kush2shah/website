import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: "Kush's Portfolio",
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
